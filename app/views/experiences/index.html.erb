<h1>All my experiences</h1>
  <%= link_to "Créer une experience", new_user_experience_path(@user) %>

<ul>
<% @experiences.each do |experience| %>
  <% if policy(experience).show? %>
    <li>Ville :<%= experience.city %></li>
    <li>Prix par heure :<%= experience.price_per_hour %></li>
    <%= link_to "Details", experience_path(experience) %>
    <%= link_to "Supprimer", user_experience_path(current_user, experience), method: :delete %>
  <% end %>
<% end %>
</ul>
