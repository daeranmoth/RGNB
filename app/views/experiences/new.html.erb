<h1 class="centered-title">Proposer une nouvelle experience</h1>
<div class="container" style="text-align: center">
  <%= simple_form_for @experience do |f| %>
    <%= f.input :price_per_hour, label:'Prix' %>
    <%= f.input :city, label:'Ville' %>
    <%= f.input :description, label:'Description' %>
    <%= f.input :category, label:'Categorie', collection: CATEGORY %>
    <%= f.submit "Valider", class: "custom-button", style: "display: inline-block" %>
  <% end %>
</div>
